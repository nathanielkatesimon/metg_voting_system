# No environment variables are required for the client in development.
# The Vite dev server proxies /api and /uploads to the backend (see vite.config.js).

# In production, the React app is served directly by the Express backend,
# so no separate client deployment configuration is needed.
